let jwt;_273‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtAuthenticated = (req,res) => {
    const cookie = req.cookies["jwt"];
    if(!cookie){
        res.redirect("/login");
        return;
    }
};